import {getNativeData, getIosProductPrice, } from '../utils/index';

const price = {
    state: 0,
    init: function() {
        if (this.state !== 1) {
            return;
        }
        getNativeData;
        getIosProductPrice;
        this.state = 1;
    },
};

export default price;
